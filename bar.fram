
data State E = 
  { get    : Unit ->[E] Int
  }

let hState s =
  handler
    let get () = effect / r => fn s => r s s
    in
    State { get }
    return x => fn _ => x
    finally f => f s
  end

#let _ = handle st with hState 5 in st.get ()

